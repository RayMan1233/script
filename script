
local Material = loadstring(game:HttpGet("https://raw.githubusercontent.com/Kinlei/MaterialLua/master/Module.lua"))()

local UI = Material.Load({
     Title = "script:##:copHUB",
     Style = 3,
     SizeX = 500,
     SizeY = 290,
     Theme = "Light"
})

local Page = UI.New({
    Title = "HOME"
})

Page.Button({
    Text = "anti bypass",
    Callback = function()
       print("Clicked!") 
    end
})

local Page = UI.New({
    Title = "MAIN"
})


Page.Button({
    Text = "auto Click sometime you getting ban or kick",
    Callback = function()
       print("Clicked!") 
    end
})

local Page = UI.New({
    Title = "Player"
})

Page.Button({
    Text = "anti kill",
    Float_Height = "15" -- Your float height | 5 is default

local i=1
G=game;
_=wait;
p=G:GetService("Players").LocalPlayer.Character
p:FindFirstChild("Humanoid").HipHeight = Float_Height
_(1.5);
t=.4;
n=2;
for i=1,i do
repeat p:FindFirstChild("Humanoid").HipHeight=Float_Height-n _(t)p:FindFirstChild("Humanoid").HipHeight=Float_Height+n _(t) until
p:FindFirstChild("Humanoid").Health == 0
end
})


